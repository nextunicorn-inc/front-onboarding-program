import { css } from '@emotion/react';

const size42 = css`
  font-size: 42px;
  line-height: 52px;
`;

const size30 = css`
  font-size: 30px;
  line-height: 40px;
`;

const size24 = css`
  font-size: 24px;
  line-height: 36px;
`;

const size18 = css`
  font-size: 18px;
  line-height: 26px;
`;

const size16 = css`
  font-size: 16px;
  line-height: 24px;
`;

const size14 = css`
  font-size: 14px;
  line-height: 20px;
`;

const size12 = css`
  font-size: 12px;
  line-height: 18px;
`;

const size10 = css`
  font-size: 10px;
  line-height: 14px;
`;

export const FontSize = {
    size42,
    size30,
    size24,
    size18,
    size16,
    size14,
    size12,
    size10
};
